extends ../components/base/layout

block variables
  - const title = "CV Yaroslav Kovalev" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = true
  - const footerMods = ""
  - const currentUrl = "index.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block main
  main.main
    //- на странице запрещен "голый" код
    //- все блоки вставляются из папки pug/components/blocks
    +cv-hero()
    +cv-contacts()
    +cv-summary()
    +cv-skills()
    +cv-projects()
    +cv-experience()
    +cv-education()
    +cv-english()

block modals
